/**
 * Component catalog — re-exported from @wikipefia/mdx-renderer (single source of truth).
 */

export {
  catalogEntries as COMPONENT_CATALOG,
  CATEGORY_META,
  knownComponentNames,
  type CatalogEntry,
  type Category,
} from "@wikipefia/mdx-renderer/registry";
