"use client";

// Promoted to the shared design system (@wikipefia/ui). Re-exported here so
// existing `@wikipefia/chat` imports keep working unchanged.
export { Button, buttonVariants, type ButtonProps } from "@wikipefia/ui";
